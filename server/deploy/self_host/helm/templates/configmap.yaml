apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "donna.fullname" . }}-config
  labels: {{- include "donna.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
