meta {
  name: Channel Messages List
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/chat/channels/{{channelId}}/messages/?limit=50
  body: none
  auth: inherit
}

params:query {
  limit: 50
  ~before: 
  ~after: 
}

headers {
  X-Workspace-Id: {{workspaceId}}
}
