meta {
  name: Read State Get
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/chat/channels/{{channelId}}/read-state/
  body: none
  auth: inherit
}

headers {
  X-Workspace-Id: {{workspaceId}}
}
