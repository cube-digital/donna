meta {
  name: Disconnect (revoke token)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/integrations/{{connectorSlug}}/disconnect/
  body: none
  auth: inherit
}

headers {
  X-Workspace-Id: {{workspaceId}}
}

docs {
  Best-effort upstream revoke + deletes the OAuthToken; cascades
  through to dependent Connection rows.
}
