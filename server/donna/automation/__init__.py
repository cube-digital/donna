"""Donna automation app.

Plan 13 §7 — home for cron-driven + policy-driven agent behaviors.
v1 ships ``Schedule`` (§7.1) + the reaction-derived feedback aggregator
(§7.3). NotificationPolicy (§7.2) was dropped from the v1 scope.
"""
default_app_config = "donna.automation.apps.AutomationConfig"
