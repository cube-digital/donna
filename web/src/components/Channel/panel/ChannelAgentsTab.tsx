// Channel agents tab — deferred. The kit's ChannelAgentsTab design is
// preserved in assets/donna-ui-kit/react/ChannelAgentsTab.jsx; the channel
// agent-install surface is not wired yet, so this renders ComingSoon.
import ComingSoon from "../../../views/ComingSoon";

export default function ChannelAgentsTab() {
  return <ComingSoon title="Agents" />;
}
