// Agents settings tab — deferred. The kit's AgentsPage design is preserved in
// assets/donna-ui-kit/react/AgentsPage.jsx; there is no /agents endpoint yet,
// so this renders the shared ComingSoon stub.
import ComingSoon from "../ComingSoon";

export default function AgentsPage() {
  return <ComingSoon title="Agents" />;
}
