# Backend base for the dev proxy (vite.config.ts uses VITE_API_PROXY_TARGET).
VITE_API_PROXY_TARGET=http://localhost:8000

# Same-origin in dev (proxied). Override in production builds.
VITE_API_BASE=
VITE_WS_BASE=
